<nav id="sidebarMenu" class="col-md-3 col-lg-2 d-md-block sidebar collapse">
    <div class="position-sticky pt-3 d-flex flex-column h-100">
        <div class="px-3 pb-3 mb-2 border-bottom border-secondary border-opacity-25">
            <h6 class="sidebar-heading d-flex justify-content-between align-items-center mb-3">
                <span><i class="bi bi-broadcast me-2"></i>SYSTEM STATUS</span>
                <span
                    class="badge {{#if isOnline}}bg-success text-success border-success{{else}}bg-danger text-danger border-danger{{/if}} bg-opacity-10 border-opacity-25 rounded-pill font-mono"
                    id="connection-badge">
                    <span class="live-indicator {{#if isOnline}}status-online{{else}}status-offline{{/if}} me-1" id="connection-dot"></span><span
                        id="connection-text">{{statusConnection}}</span>
                </span>
            </h6>
        </div>

        <ul class="nav flex-column px-3">
            <li class="nav-item mb-2">
                <a class="nav-link d-flex align-items-center {{#if (eq activePage 'dashboard')}}text-jupiter active{{else}}text-muted{{/if}}" href="/">
                    <i class="bi bi-speedometer2 me-2"></i>
                    Dashboard
                </a>
            </li>
            <li class="nav-item mb-2">
                <a class="nav-link d-flex align-items-center {{#if (eq activePage 'config')}}text-jupiter active{{else}}text-muted{{/if}}" href="/config">
                    <i class="bi bi-layers me-2"></i>
                    Virtual Inputs
                </a>
            </li>
            <li class="nav-item mb-2">
                <a class="nav-link d-flex align-items-center {{#if (eq activePage 'nat')}}text-jupiter active{{else}}text-muted{{/if}}" href="/nat">
                    <i class="bi bi-router me-2"></i>
                    NAT Config
                </a>
            </li>
        </ul>

        <div class="mt-auto p-3 text-center border-top border-secondary border-opacity-25">
            <small class="text-muted d-block" style="font-size: 0.65rem;">JUPITER TELEMETRY SYSTEM</small>
            <small class="text-muted" style="font-size: 0.65rem;">&copy; 2026 LAB SERVICE</small>
        </div>
    </div>
</nav>
