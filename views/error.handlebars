<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Erro | JUPITER</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #121212; color: #fff; height: 100vh; display: flex; align-items: center; justify-content: center; }
        .error-container { text-align: center; }
        .error-code { font-size: 5rem; font-weight: bold; color: #dc3545; }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-code">500</div>
        <h3>{{message}}</h3>
        {{#if error.stack}}
            <pre class="bg-dark text-danger p-3 mt-4 text-start small">{{error.stack}}</pre>
        {{/if}}
        <a href="/" class="btn btn-outline-light mt-4">Voltar para o Dashboard</a>
    </div>
</body>
</html>
